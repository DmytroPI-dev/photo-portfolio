# Optional public API origin used at Vite build time. Leave blank to render the
# bundled gallery data locally, or set the deployed API URL to use published
# DynamoDB metadata and CloudFront derivative URLs.
VITE_GALLERY_API_URL=

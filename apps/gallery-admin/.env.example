# Production uses the deployed public read API. Local Vite development needs no
# value because vite.config.js proxies /api to the same endpoint.
VITE_GALLERY_API_URL=https://m02dauw9h9.execute-api.eu-central-1.amazonaws.com
